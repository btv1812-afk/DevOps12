apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: mysql-pvc
  namespace: dev-ns

spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce


  resources:
    requests:
      storage: 1Gi